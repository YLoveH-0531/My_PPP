#include <iostream>
#include <vector>
#include <string>

extern void print(const std::string& str, const std::vector<int>& vec, std::ostream& os);
extern void print(const std::string& str, const std::vector<std::string>& vec, std::ostream& os);