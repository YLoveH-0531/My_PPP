  VS Code 开发环境配置（WSL + Windows VS Code）

  环境：WSL2 Ubuntu + Windows VS Code（Remote WSL 扩展）

  ── 一、settings.json ──────────────────────────────────────
  路径：C:\Users\<用户名>\AppData\Roaming\Code\User\settings.json
  追加以下三行：

    "C_Cpp.intelliSenseEngine": "disabled",   // 关闭 cpptools IntelliSense，让 clangd 接管
    "editor.formatOnSave": true,              // 保存时自动 clang-format
    "editor.snippetSuggestions": "top"        // snippet 补全排在最前

  说明：clangd 和 ms-vscode.cpptools 同时装时会打架，禁用 cpptools 的
        IntelliSense 让 clangd 负责补全/跳转，cpptools 只保留调试功能。

  ── 二、snippets/cpp.json ──────────────────────────────────
  路径：C:\Users\<用户名>\AppData\Roaming\Code\User\snippets\cpp.json
  完整内容：

    {
        "Header Guard": {
            "prefix": "hguard",
            "body": [
                "#ifndef ${TM_FILENAME_BASE/(.*)/${1:/upcase}/}_H",
                "#define ${TM_FILENAME_BASE/(.*)/${1:/upcase}/}_H",
                "",
                "$0",
                "",
                "#endif // ${TM_FILENAME_BASE/(.*)/${1:/upcase}/}_H"
            ]
        },
        "Pragma Once": {
            "prefix": "po",
            "body": [ "#pragma once", "", "$0" ]
        },
        "C++ Class": {
            "prefix": "cls",
            "body": [
                "class ${1:ClassName} {",
                "public:",
                "\t${1:ClassName}();",
                "\t~${1:ClassName}();",
                "",
                "private:",
                "\t$0",
                "};"
            ]
        },
        "Range-based for loop": {
            "prefix": "forr",
            "body": [ "for (auto& ${1:item} : ${2:container}) {", "\t$0", "}" ]
        }
    }

  ── 三、Snippet 使用方法 ────────────────────────────────────
  在 .h / .cpp 文件里输入关键词，按 Tab 展开；没反应就先按 Ctrl+Space 触发补全。

    hguard → #ifndef 头文件守护，文件名自动转大写
    po     → #pragma once（推荐，更简洁）
    cls    → class 骨架；输完类名后 Tab，三处构造/析构名同步修改
    forr   → range-based for；Tab 在变量名 → 容器名 → 函数体间跳跃

  ── 四、常用快捷键 ──────────────────────────────────────────
    F2            重命名，所有引用同步修改
    F12           跳转到定义
    Alt+F12       悬浮预览定义，不跳走
    Shift+F12     查看所有引用
    Ctrl+Shift+Space  查看当前函数参数列表

  ── 五、WSL 工具链（已装，家里新机需手动安装）──────────────
    sudo apt install build-essential gdb clangd clang-format

  ---

  基本原则

  - Drill 必做，Exercise 每章选 3-5 题
  - 每题限时：简单 20 分钟，中等 45 分钟，超时就看答案理解后继续
  - Ch12-16（FLTK GUI）直接跳过
  - Ch17-19 需要读正文，其余章节直接做题

  ---
  两个月时间表

  第 1-2 周：Ch5-7
  错误处理、写程序、完善程序
  ▎ 这三章是一个连贯的项目，建议连着做，培养"从需求到实现"的思路

  第 3-4 周：Ch8-11 (类可以看一下？试着读一下，或者做几道题感受一下)
  函数、类、I/O 流
  ▎ 类和 I/O 是重点，Primer 学过但要通过写代码真正用起来

  第 5 周：跳过 Ch12-16
  直接进 Ch17

  第 5-6 周：Ch17-19（认真读正文）
  vector 底层、数组与指针、模板与异常
  ▎ 这是 PPP 里最有价值的部分，手写简化版 vector，搞清楚内存管理

  第 7 周：Ch20-21 (可以读原文？先简略看一遍)
  容器与迭代器、STL 算法
  ▎ 直接做题，Primer 覆盖过，重点是用起来

  第 8 周：Ch22-27 选读
  浏览为主，挑感兴趣的章节做几道题，或者作为缓冲周补前面落下的内容